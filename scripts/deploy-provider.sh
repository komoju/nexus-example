#! /usr/bin/env sh
# NOTE: This will only work if you have permissions to the Heroku app
# (which you won't if you're not a Degica employee)

# Run from the git repository root
git subtree push --prefix provider heroku master